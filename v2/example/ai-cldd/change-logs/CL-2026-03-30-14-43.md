# Change Log

source_type: PROMPT
source_reference: "Altere a estrutura do projeto seguindo a especificação do sb-001 Adicione as dependencias no pom Configure o projeto para que quando a env for development executar o liquibase ao executar o projeto. Quando a env não for development o liquibase nao deve ser executado permitir a execucao do liquibase via linha de comando para que o liquibase seja executado separadamente em ferramentas CI/CD"
applied_at: 2026-03-30 14:43
applied_by: ai-agent:gpt-5.3-codex
affected_files:

- pom.xml
- src/main/resources/application.properties
- src/main/resources/application-development.properties
- src/main/resources/application-staging.properties
- src/main/resources/application-production.properties
- src/main/resources/liquibase/db.changelog-master.xml
- src/main/resources/liquibase/scripts/ddl-event-001.sql
- ai-cldd/change-logs/CL-2026-03-30-14-43.md
  summary_of_changes: |
  - Adicionada a dependencia `org.liquibase:liquibase-core` no projeto.
  - Configurado o plugin `org.liquibase:liquibase-maven-plugin` para execucao por linha de comando em pipelines CI/CD.
  - Criado o master changelog em `src/main/resources/liquibase/db.changelog-master.xml` com includeAll dos scripts SQL.
  - Criada a estrutura `src/main/resources/liquibase/scripts` com script inicial `ddl-event-001.sql`.
  - Configurado `spring.liquibase.change-log` no `application.properties`.
  - Habilitado Liquibase apenas no perfil `development` e desabilitado por padrao e nos perfis `staging` e `production`.
    SB_validation_status: |
  - SB ativo carregado: ai-cldd/docs/sb-001.md.
  - Estrutura de arquivos Liquibase criada conforme o SB-001.
  - Convencao de nomenclatura de script DDL aplicada.
  - Nenhuma violacao de regra SB detectada.

## Source

Altere a estrutura do projeto seguindo a especificação do sb-001
Adicione as dependencias no pom
Configure o projeto para que quando a env for development executar o liquibase ao executar o projeto.
Quando a env não for development o liquibase nao deve ser executado
permitir a execucao do liquibase via linha de comando para que o liquibase seja executado separadamente em ferramentas CI/CD

## Prompt Interpretation

Aplicar a estrutura e configuracao do Liquibase no projeto para execucao automatica apenas em development e habilitar execucao manual por Maven para uso em CI/CD.

## SB Validation

No SB violations detected.

## Changes Applied

- Liquibase integrado ao runtime da aplicacao.
- Execucao por ambiente controlada por profile.
- Execucao por CLI habilitada via Maven plugin.
